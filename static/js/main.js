import {
  initUpdatingSelectionUI,
  initPlotControlUI,
  initExampleButtons,
  initFunctionEditor,
  initComputeButton,
} from './ui.js';

('use strict');

// Initialize the UI
initUpdatingSelectionUI();
initPlotControlUI();
initExampleButtons();
initFunctionEditor();
initComputeButton();
