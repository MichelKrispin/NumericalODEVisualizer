import {
  initUpdatingSelectionUI,
  initExampleButtons,
  initFunctionEditor,
  initComputeButton,
} from './ui.js';

('use strict');

// Initialize the UI
initUpdatingSelectionUI();
initExampleButtons();
initFunctionEditor();
initComputeButton();
